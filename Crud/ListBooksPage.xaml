<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Crud.ViewModels"
             x:Class="Crud.ListBooksPage"
             x:DataType="vm:BookViewModel"
             Title="List Books">

    <ContentPage.BindingContext>
        <vm:BookViewModel />
    </ContentPage.BindingContext>

    <VerticalStackLayout Padding="20">
        <Label Text="Lista de Libros" FontSize="20" HorizontalOptions="Center" />

        <CollectionView ItemsSource="{Binding Books}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Titulo}" />
                        <Label Text="{Binding Genero}" />
                        <Label Text="{Binding Usuario.Name}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>
